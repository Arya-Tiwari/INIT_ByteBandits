# Set in Vercel for production builds; use the backend origin without /api or /docs.
# VITE_API_URL=https://your-backend.vercel.app
# Local npm run dev always uses the existing /api proxy.
VITE_API_URL=
