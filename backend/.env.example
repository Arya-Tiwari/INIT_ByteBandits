# Set in the backend Vercel project: comma-separated frontend origins, without paths.
# FRONTEND_ORIGINS=https://your-frontend.vercel.app
# Add exact preview origins here if needed. Local Vite origins are always allowed.
FRONTEND_ORIGINS=
